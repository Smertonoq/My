# Функции и производные: практикум

Этот практикум дополняет лекцию [«Функции и производные: как математика помогает обучать модели»](01_functions_and_derivatives_lecture.md). Его можно использовать и отдельно: перед каждым блоком приведено короткое напоминание теории, а решения спрятаны под раскрывающимися блоками.

Задачи расположены от базовых вычислений к моделям машинного обучения. Главная цель — научиться не только брать производные, но и понимать, что означают полученные числа.

---

## 1. Значения функций

Если функция задана формулой $y=f(x)$, то значение функции при конкретном $x$ находят простой подстановкой.

Например, для

$$
f(x)=2x+3
$$

имеем

$$
f(4)=2\cdot4+3=11.
$$

### Задача 1

Дана функция

$$
f(x)=2x^2-3x+1.
$$

Найдите:

1. $f(0)$;
2. $f(1)$;
3. $f(2)$;
4. $f(-1)$.

<details>
<summary><strong>Решение</strong></summary>

Подставляем значения $x$ в формулу.

$$
f(0)=2\cdot0^2-3\cdot0+1=1.
$$

$$
f(1)=2-3+1=0.
$$

$$
f(2)=2\cdot4-6+1=3.
$$

$$
f(-1)=2\cdot1+3+1=6.
$$

**Ответ:** $1,0,3,6$.

</details>

### Задача 2

Простейшая модель прогноза имеет вид

$$
\hat y=4x+10.
$$

Найдите предсказание модели при $x=3$, $x=5$ и $x=7$. Объясните, что означает коэффициент $4$.

<details>
<summary><strong>Решение</strong></summary>

$$
\hat y(3)=4\cdot3+10=22,
$$

$$
\hat y(5)=30,
$$

$$
\hat y(7)=38.
$$

Коэффициент $4$ означает: если $x$ увеличивается на единицу, прогноз модели увеличивается на $4$.

</details>

### Задача 3

Для каждой функции определите характер поведения:

$$
a(x)=3x+2,
$$

$$
b(x)=(x-1)^2,
$$

$$
c(x)=7,
$$

$$
d(x)=-2x+5.
$$

Какая функция возрастает? Какая убывает? Какая имеет минимум? Какая не меняется?

<details>
<summary><strong>Решение</strong></summary>

- $a(x)=3x+2$ возрастает, потому что коэффициент при $x$ положителен.
- $d(x)=-2x+5$ убывает, потому что коэффициент при $x$ отрицателен.
- $b(x)=(x-1)^2$ имеет минимум при $x=1$.
- $c(x)=7$ постоянна.

</details>

---

## 2. Средняя скорость изменения

Средняя скорость изменения функции на промежутке от $x_1$ до $x_2$ равна

$$
\frac{f(x_2)-f(x_1)}{x_2-x_1}.
$$

Она показывает, насколько в среднем меняется значение функции при изменении аргумента на единицу.

### Задача 4

Пусть

$$
f(x)=x^2.
$$

Найдите среднюю скорость изменения:

1. от $x=1$ до $x=3$;
2. от $x=2$ до $x=3$;
3. от $x=2$ до $x=2.1$;
4. от $x=2$ до $x=2.01$.

Что происходит с ответом, когда вторая точка приближается к $x=2$?

<details>
<summary><strong>Решение</strong></summary>

1.
$$
\frac{9-1}{3-1}=4.
$$

2.
$$
\frac{9-4}{1}=5.
$$

3.
$$
\frac{4.41-4}{0.1}=4.1.
$$

4.
$$
\frac{4.0401-4}{0.01}=4.01.
$$

При приближении второй точки к $2$ результат приближается к $4$. Это значение производной $f'(2)$.

</details>

### Задача 5

Путь тела задаётся функцией

$$
s(t)=3t^2+2.
$$

Найдите среднюю скорость на промежутке от $t=2$ до $t=2.5$.

<details>
<summary><strong>Решение</strong></summary>

$$
s(2)=3\cdot4+2=14,
$$

$$
s(2.5)=3\cdot6.25+2=20.75.
$$

Тогда

$$
v_{\text{ср}}=\frac{20.75-14}{2.5-2}=\frac{6.75}{0.5}=13.5.
$$

**Ответ:** $13.5$ единицы пути за единицу времени.

</details>

---

## 3. Базовые производные

Полезные правила:

$$
(c)'=0,
$$

$$
(x^n)'=nx^{n-1},
$$

$$
(c f(x))'=c f'(x),
$$

$$
(f(x)+g(x))'=f'(x)+g'(x).
$$

### Задача 6

Найдите производные:

1. $f(x)=7x^2$;
2. $g(x)=3x^4-2x+5$;
3. $h(x)=x^5+x^2-9$;
4. $p(x)=12$;
5. $q(x)=-4x^3+6x^2-8x+1$.

<details>
<summary><strong>Решение</strong></summary>

1.
$$
f'(x)=14x.
$$

2.
$$
g'(x)=12x^3-2.
$$

3.
$$
h'(x)=5x^4+2x.
$$

4.
$$
p'(x)=0.
$$

5.
$$
q'(x)=-12x^2+12x-8.
$$

</details>

### Задача 7

Для функции

$$
f(x)=x^3-2x
$$

найдите $f'(x)$ и $f'(2)$. Что означает знак полученного числа?

<details>
<summary><strong>Решение</strong></summary>

$$
f'(x)=3x^2-2.
$$

При $x=2$:

$$
f'(2)=3\cdot4-2=10.
$$

Производная положительна, следовательно, около $x=2$ функция возрастает.

</details>

### Задача 8

Дана функция

$$
f(x)=x^2-6x+10.
$$

1. Найдите $f'(x)$.
2. Решите уравнение $f'(x)=0$.
3. Найдите значение функции в этой точке.
4. Объясните, почему это минимум.

<details>
<summary><strong>Решение</strong></summary>

$$
f'(x)=2x-6.
$$

Решаем

$$
2x-6=0,
$$

откуда

$$
x=3.
$$

$$
f(3)=9-18+10=1.
$$

При $x<3$ производная отрицательна, функция убывает. При $x>3$ производная положительна, функция возрастает. Следовательно, в точке $x=3$ находится минимум.

**Минимум:** $f(3)=1$.

</details>

---

## 4. Цепное правило

Если функция имеет вид

$$
y=F(u(x)),
$$

то

$$
\frac{dy}{dx}=\frac{dy}{du}\cdot\frac{du}{dx}.
$$

Иными словами, нужно взять производную внешней функции и умножить её на производную внутренней.

### Задача 9

В функции

$$
f(x)=(5x-2)^3
$$

выделите внутреннюю функцию $u(x)$ и внешнюю функцию $F(u)$.

<details>
<summary><strong>Решение</strong></summary>

Внутренняя функция:

$$
u(x)=5x-2.
$$

Внешняя функция:

$$
F(u)=u^3.
$$

</details>

### Задача 10

Найдите производные:

1. $f(x)=(2x+3)^2$;
2. $g(x)=(4x-1)^3$;
3. $h(x)=(x^2+1)^2$;
4. $r(x)=(3x^2-2x+1)^4$.

<details>
<summary><strong>Решение</strong></summary>

1.
$$
f'(x)=2(2x+3)\cdot2=4(2x+3).
$$

2.
$$
g'(x)=3(4x-1)^2\cdot4=12(4x-1)^2.
$$

3.
$$
h'(x)=2(x^2+1)\cdot2x=4x(x^2+1).
$$

4.
$$
r'(x)=4(3x^2-2x+1)^3(6x-2).
$$

</details>

### Задача 11

Пусть

$$
u=2x+1,
$$

$$
y=u^2.
$$

В точке $x=3$ найдите:

1. $u$;
2. $\frac{du}{dx}$;
3. $\frac{dy}{du}$;
4. $\frac{dy}{dx}$.

<details>
<summary><strong>Решение</strong></summary>

При $x=3$:

$$
u=2\cdot3+1=7.
$$

$$
\frac{du}{dx}=2.
$$

$$
\frac{dy}{du}=2u=14.
$$

По цепному правилу:

$$
\frac{dy}{dx}=14\cdot2=28.
$$

Проверка: $y=(2x+1)^2$, поэтому

$$
y'=2(2x+1)\cdot2=4(2x+1),
$$

а при $x=3$ получаем $28$.

</details>

---

## 5. Функция ошибки

В машинном обучении функцию, которая измеряет качество предсказания, называют **функцией потерь** или **функцией ошибки**.

Один из простейших вариантов:

$$
L=(\hat y-y)^2,
$$

где $y$ — правильный ответ, а $\hat y$ — предсказание модели.

### Задача 12

Истинное значение равно

$$
y=10.
$$

Модель выдала три предсказания:

$$
\hat y_1=8,\qquad \hat y_2=11,\qquad \hat y_3=14.
$$

Вычислите квадрат ошибки для каждого предсказания. Какое предсказание лучше?

<details>
<summary><strong>Решение</strong></summary>

Для первого:

$$
L_1=(8-10)^2=4.
$$

Для второго:

$$
L_2=(11-10)^2=1.
$$

Для третьего:

$$
L_3=(14-10)^2=16.
$$

Наименьшая ошибка у второго предсказания, поэтому оно лучшее.

</details>

### Задача 13

Пусть

$$
L(w)=(w-4)^2.
$$

1. Найдите $L'(w)$.
2. Вычислите $L'(1)$.
3. Вычислите $L'(6)$.
4. В какую сторону нужно менять $w$ в каждом случае, чтобы уменьшить ошибку?

<details>
<summary><strong>Решение</strong></summary>

$$
L'(w)=2(w-4).
$$

При $w=1$:

$$
L'(1)=-6.
$$

Чтобы уменьшить $L$, нужно увеличивать $w$.

При $w=6$:

$$
L'(6)=4.
$$

Чтобы уменьшить $L$, нужно уменьшать $w$.

</details>

---

## 6. Градиентный спуск

Для одного параметра шаг градиентного спуска записывается так:

$$
w_{\text{new}}=w_{\text{old}}-\eta L'(w_{\text{old}}).
$$

### Задача 14

Пусть

$$
L(w)=(w-5)^2,
$$

начальное значение

$$
w_0=1,
$$

а скорость обучения

$$
\eta=0.1.
$$

Сделайте два шага градиентного спуска.

<details>
<summary><strong>Решение</strong></summary>

Производная:

$$
L'(w)=2(w-5).
$$

Первый шаг:

$$
L'(1)=2(1-5)=-8.
$$

$$
w_1=1-0.1\cdot(-8)=1.8.
$$

Второй шаг:

$$
L'(1.8)=2(1.8-5)=-6.4.
$$

$$
w_2=1.8-0.1\cdot(-6.4)=2.44.
$$

После двух шагов параметр приблизился к минимуму $w=5$.

</details>

### Задача 15

Для той же функции

$$
L(w)=(w-5)^2
$$

начните с $w_0=9$ при $\eta=0.1$. Сделайте два шага.

<details>
<summary><strong>Решение</strong></summary>

$$
L'(9)=2(9-5)=8.
$$

$$
w_1=9-0.1\cdot8=8.2.
$$

$$
L'(8.2)=2(8.2-5)=6.4.
$$

$$
w_2=8.2-0.1\cdot6.4=7.56.
$$

Теперь параметр движется к минимуму слева направо? Нет: начальная точка была справа от минимума, поэтому параметр уменьшается и идёт влево к $5$.

</details>

### Задача 16. Почему размер шага важен

Пусть

$$
L(w)=w^2,
$$

$$
L'(w)=2w.
$$

Начальное значение $w_0=4$.

Сделайте один шаг для трёх значений скорости обучения:

1. $\eta=0.1$;
2. $\eta=0.5$;
3. $\eta=1.1$.

Сравните результаты.

<details>
<summary><strong>Решение</strong></summary>

При $w_0=4$:

$$
L'(4)=8.
$$

1. При $\eta=0.1$:

$$
w_1=4-0.1\cdot8=3.2.
$$

Мы приблизились к нулю.

2. При $\eta=0.5$:

$$
w_1=4-0.5\cdot8=0.
$$

За один шаг попали точно в минимум.

3. При $\eta=1.1$:

$$
w_1=4-1.1\cdot8=-4.8.
$$

Мы перепрыгнули минимум, причём по модулю стали дальше от нуля. Слишком большой шаг может мешать сходимости.

</details>

---

## 7. Полный пример обучения простой модели

Пусть модель имеет вид

$$
\hat y=wx.
$$

Дан один обучающий пример:

$$
x=3,\qquad y=12.
$$

Начальный параметр:

$$
w=2.
$$

Функция ошибки:

$$
L=(\hat y-y)^2.
$$

Скорость обучения:

$$
\eta=0.02.
$$

### Задача 17

Выполните полный шаг обучения:

1. найдите предсказание $\hat y$;
2. найдите ошибку $L$;
3. запишите $L$ как функцию от $w$;
4. найдите $\frac{dL}{dw}$;
5. вычислите производную при $w=2$;
6. обновите $w$;
7. найдите новое предсказание и новую ошибку.

<details>
<summary><strong>Решение</strong></summary>

**1. Предсказание**

$$
\hat y=wx=2\cdot3=6.
$$

**2. Ошибка**

$$
L=(6-12)^2=36.
$$

**3. Ошибка как функция $w$**

$$
L(w)=(3w-12)^2.
$$

**4. Производная**

По цепному правилу:

$$
\frac{dL}{dw}=2(3w-12)\cdot3=6(3w-12).
$$

**5. При $w=2$**

$$
\frac{dL}{dw}=6(6-12)=-36.
$$

**6. Обновление параметра**

$$
w_{\text{new}}=2-0.02\cdot(-36)=2.72.
$$

**7. Новое предсказание**

$$
\hat y_{\text{new}}=2.72\cdot3=8.16.
$$

Новая ошибка:

$$
L_{\text{new}}=(8.16-12)^2=14.7456.
$$

Ошибка уменьшилась с $36$ до примерно $14.75$.

</details>

---

## 8. Задачи повышенного уровня

### Задача 18

Пусть

$$
L(w)=(2w+1-9)^2.
$$

1. Упростите выражение.
2. Найдите производную.
3. Найдите значение $w$, при котором ошибка минимальна.
4. Проверьте, что в этой точке $L=0$.

<details>
<summary><strong>Решение</strong></summary>

$$
L(w)=(2w-8)^2.
$$

$$
L'(w)=2(2w-8)\cdot2=4(2w-8)=8w-32.
$$

Для минимума:

$$
8w-32=0,
$$

$$
w=4.
$$

Проверка:

$$
L(4)=(8-8)^2=0.
$$

</details>

### Задача 19

Дана составная функция

$$
L(w)=\left((3w+1)^2-10\right)^2.
$$

Найдите $L'(w)$, не раскрывая скобки полностью.

<details>
<summary><strong>Решение</strong></summary>

Введём

$$
u=(3w+1)^2-10.
$$

Тогда

$$
L=u^2.
$$

По цепному правилу:

$$
\frac{dL}{dw}=2u\cdot\frac{du}{dw}.
$$

Далее

$$
\frac{du}{dw}=2(3w+1)\cdot3=6(3w+1).
$$

Следовательно,

$$
\boxed{L'(w)=12\left((3w+1)^2-10\right)(3w+1)}.
$$

</details>

### Задача 20. Объяснение без вычислений

Модель обучается градиентным спуском. На очередном шаге производная функции ошибки по параметру $w$ оказалась равна $-0.8$.

Что произойдёт с $w$ при положительной скорости обучения?

<details>
<summary><strong>Ответ</strong></summary>

В формуле

$$
w_{\text{new}}=w-\eta\frac{dL}{dw}
$$

производная отрицательна. Значит,

$$
-\eta(-0.8)>0,
$$

поэтому новое значение $w$ станет больше старого.

</details>

---

## 9. Итоговый мини-тест

1. Найдите $f'(x)$ для $f(x)=4x^3-5x^2+7$.
2. Найдите производную $(3x+2)^4$.
3. Что показывает знак производной?
4. Как связаны производная и минимум функции?
5. Для чего нужна функция ошибки?
6. Почему при градиентном спуске мы вычитаем производную?
7. Что произойдёт при слишком большой скорости обучения?
8. Почему цепное правило важно для нейросетей?

<details>
<summary><strong>Ответы</strong></summary>

1. $12x^2-10x$.
2. $12(3x+2)^3$.
3. Направление локального изменения функции: положительный знак соответствует росту, отрицательный — убыванию.
4. В гладком внутреннем минимуме производная обычно равна нулю; дополнительно нужно проверить изменение знака или другие условия.
5. Чтобы количественно измерять качество предсказаний модели.
6. Производная показывает направление роста, а нам нужно двигаться в сторону уменьшения ошибки.
7. Алгоритм может перепрыгивать минимум и расходиться.
8. Нейросеть состоит из последовательности функций, поэтому влияние ранних параметров на конечную ошибку вычисляют через произведение локальных производных.

</details>
